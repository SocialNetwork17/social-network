'use client'
import { Recaptcha } from '../Recaptcha'

export const RecaptchaDemo = () => {
    return (
        <div style={{ padding: '20px' }}>
            <div style={{ marginBottom: '20px' }}>
                <Recaptcha />
            </div>
        </div>
    )
}